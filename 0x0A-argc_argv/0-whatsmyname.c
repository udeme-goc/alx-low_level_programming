#include <stdio.h>

/**
 * main - program that prints the filename of the program.
 * @argc: commandline argument counter
 * @argv: command line vector
 * code by Udeme Harrison
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
